I, $(name), agree to the $(type) at $(document)
on $(date) (time zone = $(timezone)).
The SHA-256 digest for the $(type) is
$(digest)
which was computed using $(sigserver)
A copy of this signature email should appear in one's
sent-mail folder.

$(PEM)
